package common.protocol;

import server.generic.Requete;

public class RequeteLogout implements Requete {
    // Rien à mettre dedans, la classe elle-même est le signal "Je veux partir"
    public RequeteLogout() {}
}